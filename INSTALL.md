# 安装与使用说明

## 一、目录内容说明

```
jd-interview-battle-kit/
├── SKILL.md          # 核心文件：所有规则都在这里，AI 加载它即可运行
├── README.md         # 项目介绍（给人看的首页）
├── INSTALL.md        # 本文件
├── COMPATIBILITY.md  # 各 Agent 环境兼容性说明
├── templates/        # 三阶段交付模板（AI 输出时套用）
└── tests/            # 测试用例（验收用）
```

## 二、安装方式（按环境选择）

### 方式 1：Hermes Agent（原生，推荐）

把 `jd-interview-battle-kit/` 目录放入技能库：

```bash
cp -r jd-interview-battle-kit ~/.hermes/skills/career/
```

或在 Hermes 中通过技能管理创建（name: `jd-interview-battle-kit`，content 为 SKILL.md 全文，category 建议 `career`）。

> 注意：新技能对当前会话不可见（技能加载器在会话启动时初始化），新开一个会话即可加载。

### 方式 2：ChatGPT（自定义 GPT / 项目指令）

1. 打开自定义 GPT 编辑页（或项目指令）
2. 把 `SKILL.md` 的**完整内容**粘贴进 Instructions
3. 可同时把 `templates/` 三个文件粘贴为参考模板

### 方式 3：Claude / 通用 Agent（Projects / 自定义指令）

1. 把 `SKILL.md` 完整内容粘贴进项目指令（或作为项目文件上传）
2. `templates/` 与 `tests/` 作为参考文档上传

### 方式 4：任何支持上传文件的 AI

把 `SKILL.md` 作为文件上传，说一句"请按这份文档的规则运行"，即可触发。

## 三、使用流程

```
用户发来 JD（文字/截图/链接/多份）
        ↓
第一阶段：一页作战摘要（默认，不追问）
        ↓ 用户说"继续准备"
第二阶段：岗位作战地图 + 个人经历整理
        ↓ 用户说"开始练习/帮我准备面试"
第三阶段：项目故事 + 回答骨架 + 高概率问题 + 反向提问
```

**触发话术示例**

- "帮我看看这个岗位值不值得投" / "分析这份 JD" → 第一阶段
- "我适不适合这个岗位" / "帮我比较几个岗位" → 第一阶段（多岗位版）
- "继续准备" / "帮我整理岗位重点" → 第二阶段
- "帮我准备这场面试" / "帮我预测面试问题" → 第三阶段

**三阶段的边界**：不主动越级。用户只发 JD 时绝不自动进入第二、三阶段。

## 四、输出文件规则

- 所有输出使用 Markdown，不生成 Word/PDF
- 用户要文件时，文件名：`JD面试作战包_<岗位名称或多岗位对比>_<YYYY-MM-DD>.md`
- 完整文档开头永远是一页作战摘要，只附用户已进入阶段的内容

## 五、常见问题

| 问题 | 解决 |
|---|---|
| AI 开始输出长报告/逐字稿 | 用 SKILL.md 第 4.3 / 9.3 条约束：默认只给一页；回答只给骨架 |
| AI 编造了经历 | 用第 4.2 条四分类规则重新约束；测试用例 4 专门验证 |
| AI 问太多问题 | 用第 5.1 条：每轮最多 1-3 个最关键问题 |
| 装了但加载不到 | Hermes：新开会话；其他环境：确认 SKILL.md 完整粘贴未被截断 |
| 档位出现"中至高" | 用第 4.1 / 6.2 条：档位固定，只有四档 |
