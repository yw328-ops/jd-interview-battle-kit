# 多 Agent 环境兼容性检查

> 检查日期：2026-08-05
> 检查对象：`jd-interview-battle-kit` 全部文件

## 一、技术依赖面（该 Skill 使用了什么）

| 依赖项 | 是否使用 | 说明 |
|---|---|---|
| 网络请求 / API 调用 | 否 | 纯规则 + 用户输入，无外部服务 |
| 脚本 / 代码执行 | 否 | 无 .py/.sh，运行不需要执行环境 |
| 特殊字体 / 图片 / 音频 | 否 | 纯文本 Markdown |
| 插件 / 第三方库 | 否 | 零依赖 |
| 特定平台语法 | 否 | 只用标准 Markdown（GFM） |

**结论：技术依赖面为零，任何能读取 Markdown 的 AI 环境均可运行。**

## 二、逐项兼容性检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 编码 | ✅ UTF-8 | 中文内容无乱码风险 |
| Frontmatter（YAML 头） | ✅ 标准 YAML | `---` 开头，name/description/version 字段；不支持的解析时忽略即可 |
| 正文格式 | ✅ 标准 Markdown | 标题、列表、表格、引用均为 GFM 通用语法 |
| 表格 | ✅ 标准 GFM 表格 | 最多一张核心表（第一阶段），多岗位一张比较表 |
| 代码块 | ✅ 三个反引号围栏 | 用于输出模板，通用语法 |
| 文件命名 | ✅ 纯 ASCII 目录名 | `jd-interview-battle-kit/`，内部文件名全英文，中文仅出现在内容中 |
| 长度 | ✅ SKILL.md 约 10K 字符 | 远低于常见指令长度上限（ChatGPT 指令 32K 字符 / Claude 项目 200K tokens） |
| 特殊控制字符 | ✅ 无 | 无隐藏字符、无 emoji 依赖 |

## 三、各环境适配结论

| 环境 | 加载方式 | 适配结论 |
|---|---|---|
| Hermes Agent | 技能库目录 / skill_manage | ✅ 原生支持，最优 |
| ChatGPT（自定义 GPT / 项目） | Instructions 粘贴 SKILL.md 全文 | ✅ 支持；注意粘贴完整不要截断 |
| Claude（Projects） | 项目指令粘贴或上传文件 | ✅ 支持；frontmatter 会被解析为元数据，正文规则照常生效 |
| Kimi / 豆包 / 通义等国产 Agent | 上传文件或粘贴 | ✅ 支持；若忽略 frontmatter，从正文 `# JD 面试作战包` 起读取即可，不影响规则 |
| 纯 Markdown 阅读器（人读） | 直接打开 | ✅ README 即为首页说明 |

## 四、退化路径（遇到不支持的情况怎么办）

1. **环境不解析 frontmatter**：不影响。frontmatter 只是元数据，规则全在正文，从 `## 1. 这个 Skill 是做什么的` 开始读取即可。
2. **环境不渲染表格**：影响很小。第一阶段表格可以退化为四行文字（结论/能不能做/价值/风险），规则仍然成立。
3. **指令长度受限（如微信 AI 短指令）**：可只粘贴「第 4 节三个核心原则 + 第 6 节第一阶段格式 + 第 14 节最终检查」作为精简版，仍能守住最核心行为。

## 五、检查结论

**该 Skill 包为纯文本 Markdown 制品，无外部依赖，可在主流国内外 AI Agent 环境中直接运行。** 唯一需要人工注意的是：粘贴型环境（ChatGPT/国产 Agent）要确保 SKILL.md 内容完整粘贴、未被截断。
